import React from 'react';

import RegisterForm from '@/app/features/auth/register/RegisterForm';

const RegisterRootContainer = ({ children }: { children?: React.ReactNode }) => {
  return <RegisterForm />;
};

export default RegisterRootContainer;

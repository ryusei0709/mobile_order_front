import React from 'react';
import SiginInForm from './SiginInForm';

const SiginInRootContainer = ({ children }: { children?: React.ReactNode }) => {
  return <SiginInForm />;
};

export default SiginInRootContainer;
